{
  "_doc": "Solana Security Standard (SOL-0XX) per-rule metadata — by Jelleo. HAND-AUTHORED source (like hacks/hacks.json); one entry per rule, keyed by SOL id. Merged into cli/rules.json (sync-rules.js -> scanner), semgrep metadata (sync-semgrep.js), and content/rules/*.md (sync-content.js). NOT raw-fetched by consumers (not in CHECKSUMS.txt). ADVISORY ONLY: never changes whether a rule fires, nor Semgrep `severity` / SARIF `level` (both stay WARNING). tier: high|low (submission-floor for --min-tier; LOW=hygiene/defense-in-depth/operational). Tier is a NOISE FLOOR, not a claim of un-exploitability — `--min-tier high` is for cutting alert fatigue, never the only scan for an audit; calibrate real impact via severity + SEVERITY.md. severity: high|medium|low (BASELINE impact IF a true positive; calibrate per SEVERITY.md, preconditions x access, take the lower). reachability: the first reachable call-site the reporter cites to confirm exploitability. exclusions: numbered 'do NOT flag when…' clauses (cite by number when suppressing).",
  "rules": {
    "SOL-001": { "tier": "high", "severity": "high", "reachability": "the instruction handler that accepts now_slot (or any caller-supplied slot/timestamp) from instruction data" },
    "SOL-002": { "tier": "high", "severity": "high", "reachability": "the write site that updates the shared/cross-market counter from a single market's context" },
    "SOL-003": { "tier": "low", "severity": "medium", "reachability": "the wrapper function that re-implements engine logic instead of delegating to the engine" },
    "SOL-004": { "tier": "high", "severity": "high", "reachability": "the risk/margin/health computation that omits a spec-mandated term" },
    "SOL-005": { "tier": "high", "severity": "medium", "reachability": "the realloc() call site, and whether owner / size-bound / rent-exemption are checked first" },
    "SOL-006": { "tier": "high", "severity": "high", "reachability": "the privileged handler that mutates state without a Signer<> / is_signer check on the caller" },
    "SOL-007": { "tier": "high", "severity": "high", "reachability": "the AccountInfo deserialize whose owner is never compared to program_id" },
    "SOL-008": { "tier": "high", "severity": "high", "reachability": "the use of a PDA account that is never re-derived and compared via find_program_address" },
    "SOL-009": { "tier": "high", "severity": "high", "reachability": "the invoke_signed call and the missing upstream caller-authority check" },
    "SOL-010": { "tier": "high", "severity": "high", "reachability": "the init_if_needed account that holds value or authority (re-init drops it)" },
    "SOL-011": { "tier": "high", "severity": "high", "reachability": "the close= account that isn't fully drained/zeroed before close, or whose lamports go to an attacker-chosen destination" },
    "SOL-012": { "tier": "high", "severity": "medium", "reachability": "the funded account used without a rent-exempt assertion" },
    "SOL-013": { "tier": "high", "severity": "high", "reachability": "the hardcoded token-program id and the token account it is used to invoke" },
    "SOL-014": { "tier": "high", "severity": "high", "reachability": "the +/-/* on a balance/counter (attacker-influenced) without checked_*" },
    "SOL-015": { "tier": "high", "severity": "high", "reachability": "the Account<'info, T> that cross-references another account with no has_one / constraint binding" },
    "SOL-016": { "tier": "high", "severity": "high", "reachability": "the first read of the stored .bump without a canonical-bump (find_program_address) compare" },
    "SOL-017": { "tier": "high", "severity": "high", "reachability": "the raw data-buffer cast (&*account.data.borrow() / transmute) used without deserialize-then-validate" },
    "SOL-018": { "tier": "low", "severity": "low", "reachability": "the 32-ones System Program literal used instead of system_program::ID" },
    "SOL-019": { "tier": "high", "severity": "high", "reachability": "the try_deserialize_unchecked (or manual deserialize) that skips the 8-byte discriminator" },
    "SOL-020": { "tier": "high", "severity": "high", "reachability": "the SetAuthority CPI without a current-authority verification" },
    "SOL-021": { "tier": "high", "severity": "high", "reachability": "the close/resolve op guarded by a freshness/expiry condition that cannot hold once status is terminal" },
    "SOL-022": { "tier": "low", "severity": "medium", "reachability": "the counter incremented when state degrades with no decrement / settlement path" },
    "SOL-023": { "tier": "low", "severity": "low", "reachability": "the fee/penalty integer division that rounds in the user's favor" },
    "SOL-024": { "tier": "high", "severity": "high", "reachability": "the oracle price read without a staleness (publish-slot age) or confidence check" },
    "SOL-025": { "tier": "high", "severity": "high", "reachability": "the bincode::deserialize of a Clock/Rent sysvar instead of Clock::get() / Sysvar<>" },
    "SOL-026": { "tier": "high", "severity": "high", "reachability": "the two accounts that must differ but are never checked with require_keys_neq! (non-Anchor-Account paths)" },
    "SOL-027": { "tier": "high", "severity": "high", "reachability": "the ctx.remaining_accounts element read/written/invoked without owner/key/signer validation" },
    "SOL-028": { "tier": "high", "severity": "high", "reachability": "the swap/withdraw/settle output derived with no caller min-out / max-in bound" },
    "SOL-029": { "tier": "high", "severity": "medium", "reachability": "the send path with skipPreflight: true (or no simulateTransaction before a mainnet send)" },
    "SOL-030": { "tier": "low", "severity": "low", "reachability": "the hardcoded microLamports priority-fee constant" },
    "SOL-031": { "tier": "high", "severity": "medium", "reachability": "the Jupiter quote consumed (swap built) without a quoteResponse.contextSlot freshness check" }
  }
}
