# Excluded from the .vsix. NOTE: engine/ is intentionally NOT ignored — it is the
# vendored scanner the extension runs at runtime (regenerated by vscode:prepublish).
scripts/**
test/**
.gitignore
.vscodeignore
**/*.map
.vscode/**
