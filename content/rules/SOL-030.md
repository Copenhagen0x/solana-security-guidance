# SOL-030 · Static priority fee

Hardcoded `microLamports` priority fee → underpays in congestion or overpays when idle. Fix: derive from `getRecentPrioritizationFees()` and clamp.

**Enforcement — machine-checkable.** A deterministic pattern flags this automatically in the [CLI](../../cli), the [GitHub Action](../../action.yml), the [Semgrep ruleset](../../semgrep), the [VS Code extension](../../extensions/vscode), and the [MCP server](../../mcp).

**Value tier — LOW.** LOW — hygiene / defense-in-depth / operational (filtered out by `--min-tier high`)

**Baseline severity — low.** A starting point, not a verdict: calibrate by preconditions × access level (take the lower) per [SEVERITY.md](../../SEVERITY.md).

**Reachability anchor.** To confirm this is exploitable (not merely present), cite the hardcoded microLamports priority-fee constant.

**Do NOT flag when** — cite the number when suppressing a finding:

1. the priority fee is derived from getRecentPrioritizationFees() / a fee oracle and clamped to a sane max
2. microLamports: 0 (the intentional no-priority opt-out) or a documented test/config default

## Real exploits in this class

No publicly catalogued exploit in the [Hacks Database](../../hacks/README.md) maps to this class yet. The rule is drawn from documented Solana audit patterns, not a specific in-the-wild loss.

## See it in code

Paired vulnerable / fixed snippet: [`examples/sol_030_static_priority_fee/`](../../examples/sol_030_static_priority_fee/).

---
*Part of the [Solana Security Standard](../../README.md). Full rule text: [SOL-030](../../claude-security-guidance.md#sol-030--static-priority-fee).*
